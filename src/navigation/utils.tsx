import React from 'react';
import {Text} from 'native-base';
export const tabs = {
  eventsList: 'Events',
  calendarView: 'Calendar',
};
export const routes = {
  main: 'main',
  createEvent: 'Create Event',
  tabs,
};
